# PocketBase hooks

Server-side PocketBase hooks belong in this directory. The automatic NAS
deployment mounts this directory read-only into the application container.

Do not commit credentials, member exports, bank statements, or database files.
